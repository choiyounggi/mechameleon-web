{
  "name": "stickmeleon",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "build": "npm run build --ws --if-present",
    "test": "npm run test --ws --if-present"
  }
}
